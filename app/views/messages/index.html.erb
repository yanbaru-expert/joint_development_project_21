<%= link_to "投稿画面へ", new_message_path %>
<% @messages.each dp |message| %>
    <p>
        <%= message.title %>
        <%= message.content %>
        <%= link_to "詳細", messages_path(message.id)>
        <%= link_to "編集", edit_message_path(message.id)>
    </p>
<% end %>